#include <stdio.h>

/**
 * name - return the argument passed to pirnt the object
 * Return: nothing
 */
int main(int argc __attribute__((unused)), char* argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
