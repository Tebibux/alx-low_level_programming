#include "main.h"

/**
 * name - return the argument passed to pirnt the object
 * Return: nothing
 */
int name(int argc, char* argv[])
{
	if (argc != '\0' && argc == 0)
	{
		_putchar("%s", argv[i]);
	}
}
