#include "main.h"

/**
 * name - return the argument passed to pirnt the object
 * @i: the assigned intiger
 * Return: nothing
 */
int name(int argc, char* argv[])
{
	int i;

	i = 0;
	if (i != '\0' && i == 0)
	{
		_putchar("%s", argv[i]);
	}
}
