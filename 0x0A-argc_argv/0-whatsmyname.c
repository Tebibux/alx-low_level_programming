#include <stdio.h>

/**
 * main - return the argument passed to pirnt the object
 * Return: 0 if success
 */
int main(int argc __attribute__((unused)), char* argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
