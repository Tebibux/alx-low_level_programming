#include <stdio.h>

/**
 * main - Prints the number single digit to the bese 10 starting from 0
 *
 * Return: 0 if succuss
 *
 */
int main(void)
{
	int i = 0;
	while (i < 10)
	{
		printf(i);
	}
	printf('\n');
	return (0);
}
