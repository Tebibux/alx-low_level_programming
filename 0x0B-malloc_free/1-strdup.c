#include "main.h"
#include <stdlib.h>

/**
 * _strdup - return a pointer to a newly allocated space
 * @str: pointer initger
 * Return: return pointer
 */
char *_strdup(char *str)
{
	int *c;
	
	if (str == NULL)
	{
		return (NULL);
	}
	c = malloc(char *
}
