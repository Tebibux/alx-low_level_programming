#include "main"
/**
 * print_most_numbers - prints numbers between 0 to 9
 * (neither 2 nor 4).
 * Return: no return.
 */
void print_most_numbers(void)
{
	int i = 48;

	while (i < 58)
	{
		if (ch != 50 && ch != 52)
			_putchar(ch);
		i++;
	}
	_putchar('\n');
}
