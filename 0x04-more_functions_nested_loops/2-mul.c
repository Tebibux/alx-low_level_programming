#include "main.h"

/**
 * mul - checks if parameter is an uppercase character.
 * @a: input intiger.
 * @b: input intiger.
 * Return: return the multipilication of i*j.
 */
int mul(int a, int b)
{
	return (a*b);
}
